# 🚀 クイックスタートガイド

## 5分でできる！Gitコミット履歴から残業証拠を作成

### ステップ1: GitHubトークンを取得（5分）

1. **アカウント1のトークン取得**
   - https://github.com/settings/tokens/new にアクセス
   - Note: `Overtime Evidence Collector`
   - Expiration: `90 days`
   - Select scopes: ✅ `repo` (Full control of private repositories)
   - `Generate token` をクリック
   - トークンをコピー（後で使えなくなるので必ずコピー！）

2. **アカウント2のトークン取得**
   - 別のGitHubアカウントでログイン
   - 上記の手順を繰り返し

### ステップ2: 設定ファイル作成（2分）

```bash
cd git_analyzer

# サンプルをコピー
copy github_config.json.sample github_config.json

# メモ帳で編集
notepad github_config.json
```

**編集内容:**
```json
{
  "tokens": [
    "ghp_実際のトークン1をここに貼り付け",
    "ghp_実際のトークン2をここに貼り付け"
  ],
  "usernames": [
    "あなたのGitHubユーザー名1",
    "あなたのGitHubユーザー名2"
  ]
}
```

保存して閉じる

### ステップ3: データ収集実行（1分）

```bash
# バッチファイルをダブルクリック
run_github_analyzer.bat
```

または

```bash
python github_commit_analyzer.py
```

**出力例:**
```
GitHub Commit History Analysis - Summary Report
================================================================

【分析対象期間】
  開始: 2023-01-15
  終了: 2025-11-11

【総合統計】
  総コミット数: 1,234 件
  時間外コミット数: 567 件 (45.9%)
  深夜コミット数: 89 件
  休日コミット数: 123 件
  総プロジェクト数: 15 個
```

### ステップ4: データ確認（1分）

```bash
# CSVをExcelで開く
start github_commits_evidence.csv

# サマリーをテキストエディタで開く
notepad github_commits_summary.json
```

### ステップ5: GUIにインポート（2分）

#### 方法A: プレビューで確認

```bash
cd ..
python git_import.py git_analyzer/github_commits_evidence.csv 0053629 --preview
```

#### 方法B: 実際にインポート

```bash
python git_import.py git_analyzer/github_commits_evidence.csv 0053629
```

#### 方法C: GUIから操作（将来実装）

```bash
cd ..
python gui.py
# メニュー → Git履歴インポート
```

---

## 📊 ローカルGitリポジトリからの収集（オプション）

GitHubに上げていないローカルリポジトリも分析したい場合:

### ステップ1: 設定ファイル作成

```bash
cd git_analyzer
copy git_repos_config.json.sample git_repos_config.json
notepad git_repos_config.json
```

**編集内容:**
```json
{
  "repo_paths": [
    "C:/Users/imao3/Documents/GitHub/timeclock",
    "C:/Users/imao3/Documents/GitHub/timeclock_web",
    "C:/Users/imao3/Documents/GitHub/PermitChangeDocumentGenerator"
  ],
  "author_names": [
    "あなたの名前",
    "別の名前（もしあれば）"
  ],
  "hourly_rate": 2000,
  "overtime_rate": 1.25,
  "weekend_rate": 1.35,
  "late_night_rate": 1.5
}
```

**Git設定名の確認方法:**
```bash
git config user.name
```

### ステップ2: 実行

```bash
run_git_estimator.bat
```

または

```bash
python git_work_time_estimator.py
```

---

## 🎯 よくある質問

### Q1: GitHubトークンが無効と言われる

**A:** トークンの有効期限を確認してください。
```bash
# GitHubにログイン → Settings → Developer settings → Personal access tokens
# 期限切れの場合は再生成
```

### Q2: コミットが見つからない

**A:** ユーザー名を確認してください。
```bash
# GitHubのプロフィールページで確認
https://github.com/あなたのユーザー名
```

### Q3: CSVが文字化けする

**A:** Excelで開く際にエンコーディングを指定:
1. Excelを起動
2. データ → テキストファイルから → 元のファイルの形式: `65001: Unicode (UTF-8)`

### Q4: APIレート制限に引っかかった

**A:** 1時間待つか、スクリプトに待機時間を追加:
```python
# github_commit_analyzer.py の該当箇所
time.sleep(1)  # 各リクエスト間に1秒待機
```

### Q5: 2つのアカウントのトークンを統合できない

**A:** それぞれのアカウントで個別に実行して、CSVを手動で結合:
```bash
# アカウント1
python github_commit_analyzer.py
move github_commits_evidence.csv account1_commits.csv

# 設定ファイルをアカウント2用に変更
# アカウント2
python github_commit_analyzer.py
move github_commits_evidence.csv account2_commits.csv

# CSVを結合（Excelで開いてコピペ）
```

---

## 💡 推定作業時間の精度を上げる

### コミットメッセージを丁寧に書く

**良い例:**
```
feat: ユーザー認証機能を実装（OAuth2.0対応）

- Google/Facebook ログイン追加
- セッション管理実装
- 権限チェック機能追加
```

**悪い例:**
```
update
```

### こまめにコミットする

- 大きな変更を1コミットにまとめない
- 機能ごとに分割してコミット
- 作業の区切りで必ずコミット

### ブランチ戦略

- feature/機能名 でブランチを切る
- プロジェクト名がわかりやすくなる

---

## 🔐 セキュリティチェックリスト

- [ ] `github_config.json` を `.gitignore` に追加済み
- [ ] `git_repos_config.json` を `.gitignore` に追加済み
- [ ] トークンを絶対に公開しない
- [ ] 個人デバイスで実行（会社PCは使わない）
- [ ] 生成されたCSVはクラウドにバックアップ
- [ ] 90日後にトークンを再生成

---

## 📞 困ったときは

### ログを確認

```bash
# 直近のエラーを確認
python github_commit_analyzer.py 2>&1 | tee error.log
```

### 設定ファイルのチェック

```bash
# JSONの文法チェック
python -m json.tool github_config.json
```

### テスト実行

```bash
# 小規模なテスト
python -c "import requests; print(requests.get('https://api.github.com/rate_limit').json())"
```

---

## ✅ 最終チェック

実行前に確認:

- [ ] Python 3.7以上がインストール済み
- [ ] `pip install requests pandas openpyxl matplotlib` 実行済み
- [ ] GitHubトークンが有効
- [ ] ユーザー名が正確
- [ ] `.gitignore` に設定ファイルを追加
- [ ] 個人デバイスで実行予定
- [ ] バックアップ先を決定

---

**準備OK? では実行しましょう！**

```bash
cd git_analyzer
run_github_analyzer.bat
```

**成功したら次は:**
```bash
cd ..
python git_import.py git_analyzer/github_commits_evidence.csv 0053629 --preview
```

**祝！残業証拠データの完成です！** 🎉
